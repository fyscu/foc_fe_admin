import React, { Component as Cp } from "react";
import styles from "./App.module.css";

/**@once */
export default class Login extends Cp{
    render() :React.ReactNode{
        return(
            <div className={styles.fullScreen}>
                
            </div>
        );
    }
}